package net.kodeninja.jem.server.storage;

public enum MetadataType {
	DateAdded,
	Artist, Title, Genre, Year, Description, Set, SetPosition,
	Length, Actor, Director, Producer, 
	FileSize, Format, BitRate, FrameRate, AspectRatio, Width, Height,
	Rating
}
